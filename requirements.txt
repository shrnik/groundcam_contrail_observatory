# Core
pandas==2.3.3
opencv-python==4.12.0.88
PyYAML==6.0.2

# Geometry / projection
pyproj==3.7.2
pymap3d==3.2.0

# Computer vision
scikit-image==0.25.2  # skimage.filters.frangi in detection_utils (was missing from requirements)

# Networking
httpx==0.28.1
requests==2.32.5

# Storage
duckdb==1.1.3
azure-storage-blob==12.25.1

# CLI / progress
tqdm==4.67.1
dotenv
# # non_live pipeline only
# torch  # version depends on your CUDA setup — install separately if needed
# transformers
pillow==11.3.0
